# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

---

## フェーズ1: tracking.py 新規作成

- [x] `src/task_mcp/tracking.py` を新規作成
  - [x] `track` デコレータ（`@functools.wraps` で `__name__`・`__doc__` を保持）
  - [x] `_append_log()` — JSONL ファイルへの追記（失敗しても例外を握り潰す）
  - [x] `read_stats()` — ログ集計して文字列を返す

## フェーズ2: server.py 変更

- [x] `tracking.py` から `track` をインポート
- [x] 全 22 ツールに `@track` デコレータを追加
- [x] `get_mcp_stats` ツールを追加（`read_stats()` を呼ぶだけ）

## フェーズ3: テスト追加

- [x] `tests/test_mcp_server.py` に追記
  - [x] `track` デコレータが JSONL ファイルに書き込むことを確認
  - [x] `read_stats()` が正しい集計文字列を返すことを確認
  - [x] `get_mcp_stats` ツール呼び出しテスト（`call_tool` 経由）
  - [x] ログファイルが存在しない場合に「ログがまだありません」を返すことを確認
  - [x] `get_mcp_stats` が EXPECTED_TOOLS に含まれることを確認

## フェーズ4: 品質チェック

- [x] `uv run pytest` — 全テスト通過を確認（163件）
- [x] `uv run pyright src tests` — 型エラーゼロを確認

## フェーズ5: ドキュメント更新

- [x] `README.md` のツール一覧に `get_mcp_stats` を追記

---

## 実装後の振り返り

### 実装完了日
2026-06-10

### 計画と実績の差分

**計画と異なった点**:
- `test_track_writes_log` で `_LOG_PATH` をモンキーパッチする実装が煩雑だったため、`tracking._LOG_PATH` を直接差し替える方式にした

**新たに必要になったタスク**:
- なし

### 学んだこと

**技術的な学び**:
- FastMCP のミドルウェアは HTTP トランスポート専用で stdio には効かないため、Python デコレータで横断処理するのが正解
- `@functools.wraps` で `__name__`・`__doc__` を保持しないと FastMCP のツール名・description 登録が壊れる

### リリース判断

**前提条件の確認**:
- [x] 全テスト通過（`uv run pytest`）— 163件
- [x] 型チェック通過（`uv run pyright src tests`）

**評価**:

| 観点 | 評価 |
|---|---|
| 今回の変更はユーザーにとって価値のあるまとまりか | Yes |
| 未解決の重大バグはないか | なし |
| 適切なバージョン種別 | PATCH（既存機能への横断的な記録追加） |

**提案**:
`v0.8.2` へのバージョンアップを提案。
