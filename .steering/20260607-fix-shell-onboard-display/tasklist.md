# タスクリスト

## フェーズ1: onboard.py の表示修正

- [x] ルーティーンセクションを修正
  - [x] 全ルーティーン（done 含む）を取得して件数を計算
  - [x] pending のみ `○ [rN]` 形式で表示
  - [x] ヒント文を追加

- [x] 今とりかかるべきタスクを修正
  - [x] 番号付きリスト形式に変更
  - [x] `[status] ID  タイトル  (Project)` 形式で表示
  - [x] ヒント文を追加

- [x] 全タスクセクションを修正
  - [x] `uc.list_all_projects()` で全プロジェクト横断取得
  - [x] open + in_progress フィルタを適用
  - [x] プロジェクト別グループ表示（Inbox は最後）

## フェーズ2: 品質チェック

- [x] `uv run pytest`（148件全通過）
- [x] `uv run pyright src`（エラー0件）
- [x] 手動動作確認: `uv run task-py onboard` の表示を確認

## フェーズ3: 振り返り

- [x] 振り返りを記録

---

## 実装後の振り返り

### 実装完了日
2026-06-07

### 計画と実績の差分
- Rich の `[tag]` エスケープ処理で `\[` はSyntaxWarningが出るため、`rich.markup.escape()` を使う方法に変更した

### 学んだこと
- Rich で `[text]` を文字通り表示するには `rich.markup.escape()` を使う

### リリース判断

**前提条件の確認**:
- [x] 全テスト通過（148件）
- [x] 型チェック通過（エラー0件）

**評価**:

| 観点 | 評価 |
|---|---|
| 今回の変更はユーザーにとって価値のあるまとまりか | Yes |
| 未解決の重大バグはないか | なし |
| 適切なバージョン種別 | PATCH |

**提案**: `v0.7.2` へのバージョンアップを提案。理由: 表示バグ修正のため PATCH。
